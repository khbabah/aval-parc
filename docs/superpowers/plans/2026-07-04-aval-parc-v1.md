# Aval Parc v1 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Livrer Aval Parc v1 : fork suiveur de Snipe-IT v8.6.3 rebrandé « Aval Parc », avec seeders CNC (catégories, statuts, champs personnalisés véhicules/biomédical), commande d'installation `aval:install`, et déploiement Docker local hors-ligne.

**Architecture:** Fork Git à deux branches (`upstream-master` = miroir Snipe-IT, `main` = upstream + couche Aval). Toutes les customisations v1 sont des **fichiers nouveaux** (seeders `database/seeders/Aval/`, commande `app/Console/Commands/Aval/`, dossier `deploy/`) — aucun fichier upstream n'est modifié. Le rebranding passe par la table `settings` de Snipe-IT (mécanisme natif).

**Tech Stack:** Laravel 12 / PHP ^8.2 (local : PHP 8.4.8), MariaDB 11.4, Docker Compose, PHPUnit (sqlite in-memory via `.env.testing`).

## Global Constraints

- Base upstream : tag **`v8.6.3`** de `https://github.com/grokability/snipe-it.git` — jamais `master`.
- **Aucun fichier upstream modifié en v1.** Uniquement des fichiers nouveaux. Si un patch upstream devient inévitable : STOP, le documenter dans `docs/UPSTREAM_PATCHES.md` et demander validation.
- Seeders **idempotents** : `firstOrCreate`/`updateOrCreate` uniquement. **Jamais `truncate`** (les seeders tourneront sur des bases de production).
- Nom produit exact partout : **« Aval Parc »**.
- Chaînes visibles par l'utilisateur final : en **français**.
- Licence **AGPL-3.0** : le fichier `LICENSE` upstream reste intact ; le pied de page pointe vers le code source public.
- Messages de commit : anglais, style conventionnel (`feat:`, `docs:`, `chore:`), suffixés `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- Tests : `php artisan test <chemin> --stop-on-failure`. La suite upstream doit rester verte.

## File Structure (fichiers Aval créés par ce plan)

| Fichier | Responsabilité |
|---|---|
| `database/seeders/Aval/AvalBrandingSeeder.php` | Rebranding : settings « Aval Parc », fr-FR, MRU, footer AGPL |
| `database/seeders/Aval/CncCategorySeeder.php` | Catégories + statuts CNC |
| `database/seeders/Aval/CncCustomFieldSeeder.php` | Champs personnalisés + fieldsets Véhicule / Biomédical |
| `database/seeders/Aval/CncSeeder.php` | Orchestrateur : appelle les 3 seeders ci-dessus |
| `app/Console/Commands/Aval/AvalInstall.php` | `php artisan aval:install` : migrations + admin + seeders |
| `tests/Feature/Aval/AvalBrandingSeederTest.php` | Tests seeder branding |
| `tests/Feature/Aval/CncSeedersTest.php` | Tests catégories/statuts/champs + idempotence |
| `tests/Feature/Aval/AvalInstallTest.php` | Test de la commande complète |
| `deploy/docker-compose.yml` | Prod : build image locale `aval-parc` + MariaDB |
| `deploy/.env.example` | Modèle d'environnement prod |
| `deploy/install.sh` | Installation initiale sur le serveur clinique |
| `deploy/backup.sh` | Sauvegarde base + uploads, rotation 14 jours |
| `deploy/export-images.sh` | `docker save` pour transfert USB (serveur hors-ligne) |
| `deploy/README.md` | Procédure d'installation/sauvegarde/restauration |
| `docs/UPSTREAM_PATCHES.md` | Registre des patchs upstream (vide en v1) |
| `docs/MISE_A_JOUR_UPSTREAM.md` | Procédure de merge d'une release Snipe-IT |
| `README-AVAL.md` | Présentation produit, mention AGPL, liens docs |

---

### Task 1: Fork suiveur — brancher le dépôt sur Snipe-IT v8.6.3

**Files:**
- Modify: dépôt Git local (remotes, branches) — aucun fichier source.

**Interfaces:**
- Produces: branche `main` = arbre complet Snipe-IT v8.6.3 + `docs/superpowers/` ; branche `upstream-master` = miroir pur ; remote `upstream` configuré. Toutes les tâches suivantes travaillent sur `main`.

- [ ] **Step 1 : Ajouter le remote upstream et récupérer le tag**

```bash
cd /Users/khaled/Khaled_ALL/INFO_PROJECTS/GitHub-Khbabah/aval-inventory
git remote add upstream https://github.com/grokability/snipe-it.git
git fetch upstream --tags
```
Attendu : le fetch télécharge l'historique (peut prendre quelques minutes).

- [ ] **Step 2 : Créer la branche miroir `upstream-master`**

```bash
git branch upstream-master v8.6.3
```

- [ ] **Step 3 : Merger v8.6.3 dans `main`**

Le dépôt local n'a qu'un commit docs ; les historiques sont indépendants.

```bash
git checkout main
git merge v8.6.3 --allow-unrelated-histories -m "chore: merge Snipe-IT v8.6.3 as upstream base

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```
Attendu : merge sans conflit (les chemins `docs/superpowers/` n'existent pas upstream).

- [ ] **Step 4 : Vérifier l'état du fork**

```bash
ls LICENSE artisan composer.json docs/superpowers/specs/
git log --oneline -3
```
Attendu : `LICENSE` (AGPL) présent, spec toujours là, dernier commit = merge.

- [ ] **Step 5 : Créer le repo GitHub privé et pousser**

```bash
gh repo create Khbabah/aval-parc --private --source=. --remote=origin
git push -u origin main upstream-master --tags
```
Attendu : repo `Khbabah/aval-parc` créé, deux branches poussées. (Le dossier local garde son nom `aval-inventory` ; le renommer est optionnel et hors session.)

---

### Task 2: Environnement de dev + harnais de tests validé

**Files:**
- Create: `.env.testing` (copie locale, déjà dans `.gitignore` upstream)

**Interfaces:**
- Produces: `composer install` fait, `php artisan test <fichier>` fonctionnel sur sqlite in-memory. Toutes les tâches TDD suivantes en dépendent.

- [ ] **Step 1 : Installer les dépendances PHP**

```bash
composer install --no-interaction
```
Si erreur d'extension manquante (ex. `ext-ldap` fréquent sur macOS) :
```bash
composer install --no-interaction --ignore-platform-req=ext-ldap
```
Attendu : `vendor/` créé sans erreur fatale.

- [ ] **Step 2 : Configurer l'environnement de test sqlite**

```bash
cp .env.testing.example .env.testing
```
Puis dans `.env.testing`, vérifier/forcer ces deux lignes (le reste ne change pas) :
```
DB_CONNECTION=sqlite_testing
APP_KEY=base64:glJpcM7BYwWiBggp3SQ/+NlRkqsBQMaGEOjemXqJzOU=
```

- [ ] **Step 3 : Valider le harnais en exécutant des tests upstream**

```bash
php artisan test tests/Unit --stop-on-failure
```
Attendu : PASS (suite Unit verte). Si un test upstream échoue à ce stade, STOP : le problème vient de l'environnement, pas de notre code — corriger avant de continuer.

- [ ] **Step 4 : Commit (rien à committer normalement)**

`.env.testing` est ignoré par git. Vérifier : `git status` → arbre propre.

---

### Task 3: Seeder de branding « Aval Parc »

**Files:**
- Create: `database/seeders/Aval/AvalBrandingSeeder.php`
- Test: `tests/Feature/Aval/AvalBrandingSeederTest.php`

**Interfaces:**
- Consumes: modèle upstream `App\Models\Setting` (ligne unique de la table `settings`).
- Produces: classe `Database\Seeders\Aval\AvalBrandingSeeder` (méthode `run(): void`), appelée par `CncSeeder` (Task 5) et `aval:install` (Task 6). Fonctionne que la ligne `settings` existe déjà ou non.

- [ ] **Step 1 : Écrire le test qui échoue**

Créer `tests/Feature/Aval/AvalBrandingSeederTest.php` :

```php
<?php

namespace Tests\Feature\Aval;

use App\Models\Setting;
use Database\Seeders\Aval\AvalBrandingSeeder;
use Tests\TestCase;

class AvalBrandingSeederTest extends TestCase
{
    public function test_seeder_creates_settings_row_when_none_exists()
    {
        Setting::query()->delete();

        $this->seed(AvalBrandingSeeder::class);

        $settings = Setting::first();
        $this->assertNotNull($settings);
        $this->assertEquals('Aval Parc', $settings->site_name);
        $this->assertEquals('fr-FR', $settings->locale);
        $this->assertEquals('MRU', $settings->default_currency);
        $this->assertStringContainsString('github.com/Khbabah/aval-parc', $settings->footer_text);
    }

    public function test_seeder_updates_existing_settings_without_duplicating()
    {
        Setting::query()->delete();
        Setting::factory()->create(['site_name' => 'Snipe-IT Demo']);

        $this->seed(AvalBrandingSeeder::class);
        $this->seed(AvalBrandingSeeder::class); // idempotence

        $this->assertEquals(1, Setting::count());
        $this->assertEquals('Aval Parc', Setting::first()->site_name);
    }
}
```

- [ ] **Step 2 : Vérifier l'échec**

```bash
php artisan test tests/Feature/Aval/AvalBrandingSeederTest.php
```
Attendu : FAIL — `Class "Database\Seeders\Aval\AvalBrandingSeeder" not found`.

- [ ] **Step 3 : Implémenter le seeder**

Créer `database/seeders/Aval/AvalBrandingSeeder.php` :

```php
<?php

namespace Database\Seeders\Aval;

use App\Models\Setting;
use Illuminate\Database\Seeder;

/**
 * Rebranding Aval Parc via les settings natifs de Snipe-IT.
 * Idempotent : ne crée jamais de doublon, met à jour la ligne existante.
 */
class AvalBrandingSeeder extends Seeder
{
    public function run(): void
    {
        $settings = Setting::first() ?? new Setting;

        $settings->site_name = 'Aval Parc';
        $settings->brand = 1; // 1 = texte seul; le client pourra téléverser un logo dans l'UI
        $settings->locale = 'fr-FR';
        $settings->default_currency = 'MRU';
        $settings->support_footer = 'off';
        $settings->version_footer = 'off';
        $settings->footer_text = 'Aval Parc — basé sur [Snipe-IT](https://snipeitapp.com) (AGPL-3.0) · [Code source](https://github.com/Khbabah/aval-parc)';

        // Valeurs minimales requises si la ligne n'existe pas encore (avant le setup web)
        $settings->per_page = $settings->per_page ?? 20;
        $settings->auto_increment_assets = $settings->auto_increment_assets ?? 1;
        $settings->pwd_secure_min = $settings->pwd_secure_min ?? '10';
        $settings->default_avatar = $settings->default_avatar ?? 'default.png';

        $settings->save();
    }
}
```

- [ ] **Step 4 : Vérifier le pass**

```bash
php artisan test tests/Feature/Aval/AvalBrandingSeederTest.php
```
Attendu : PASS (2 tests). Si la sauvegarde échoue sur une règle de validation du modèle `Setting`, lire le message et ajouter le champ requis manquant avec une valeur par défaut explicite (même approche que `per_page` ci-dessus).

- [ ] **Step 5 : Commit**

```bash
git add database/seeders/Aval/AvalBrandingSeeder.php tests/Feature/Aval/AvalBrandingSeederTest.php
git commit -m "feat: add Aval Parc branding seeder"
```

---

### Task 4: Seeders CNC — catégories, statuts, champs personnalisés

**Files:**
- Create: `database/seeders/Aval/CncCategorySeeder.php`
- Create: `database/seeders/Aval/CncCustomFieldSeeder.php`
- Test: `tests/Feature/Aval/CncSeedersTest.php`

**Interfaces:**
- Consumes: modèles upstream `Category`, `Statuslabel`, `CustomField`, `CustomFieldset`, `User`. La sauvegarde d'un `CustomField` crée automatiquement la colonne `_snipeit_*` sur la table `assets` (mécanisme natif).
- Produces: `Database\Seeders\Aval\CncCategorySeeder` et `Database\Seeders\Aval\CncCustomFieldSeeder` (méthodes `run(): void`), idempotents, utilisables sans admin préexistant (ils en créent un si besoin est absent — non : ils prennent le premier superuser existant, requis). Fieldsets produits : « Véhicule » et « Équipement biomédical », à associer aux modèles d'actifs dans l'UI.

- [ ] **Step 1 : Écrire les tests qui échouent**

Créer `tests/Feature/Aval/CncSeedersTest.php` :

```php
<?php

namespace Tests\Feature\Aval;

use App\Models\Category;
use App\Models\CustomField;
use App\Models\CustomFieldset;
use App\Models\Statuslabel;
use App\Models\User;
use Database\Seeders\Aval\CncCategorySeeder;
use Database\Seeders\Aval\CncCustomFieldSeeder;
use Tests\TestCase;

class CncSeedersTest extends TestCase
{
    private function seedTwice(string $seeder): void
    {
        User::factory()->firstAdmin()->create();
        $this->seed($seeder);
        $this->seed($seeder); // idempotence : aucun doublon
    }

    public function test_category_seeder_creates_cnc_categories_and_statuses()
    {
        $this->seedTwice(CncCategorySeeder::class);

        foreach (['Équipement biomédical', 'Véhicule', 'Informatique', 'Mobilier'] as $name) {
            $this->assertEquals(1, Category::where('name', $name)->where('category_type', 'asset')->count(), $name);
        }

        $this->assertEquals(1, Statuslabel::where('name', 'En service')->where('deployable', 1)->count());
        $this->assertEquals(1, Statuslabel::where('name', 'En maintenance')->count());
        $this->assertEquals(1, Statuslabel::where('name', 'En panne')->count());
        $this->assertEquals(1, Statuslabel::where('name', 'Réformé')->where('archived', 1)->count());
    }

    public function test_custom_field_seeder_creates_vehicle_and_biomedical_fieldsets()
    {
        $this->seedTwice(CncCustomFieldSeeder::class);

        $vehicule = CustomFieldset::where('name', 'Véhicule')->first();
        $this->assertNotNull($vehicule);
        $this->assertEquals(
            ['Immatriculation', 'Kilométrage', 'Échéance assurance', 'Échéance vignette', 'Chauffeur affecté'],
            $vehicule->fields->pluck('name')->all()
        );

        $biomedical = CustomFieldset::where('name', 'Équipement biomédical')->first();
        $this->assertNotNull($biomedical);
        $this->assertEquals(
            ['Criticité clinique', 'Dernière calibration', 'Prochaine calibration', 'Contrat de maintenance'],
            $biomedical->fields->pluck('name')->all()
        );

        $this->assertEquals('date', CustomField::where('name', 'Échéance assurance')->first()->format);
        $this->assertStringContainsString('Critique', CustomField::where('name', 'Criticité clinique')->first()->field_values);

        // Idempotence : 9 champs au total, pas 18
        $this->assertEquals(9, CustomField::count());
    }
}
```

- [ ] **Step 2 : Vérifier l'échec**

```bash
php artisan test tests/Feature/Aval/CncSeedersTest.php
```
Attendu : FAIL — classes seeders introuvables.

- [ ] **Step 3 : Implémenter `CncCategorySeeder`**

Créer `database/seeders/Aval/CncCategorySeeder.php` :

```php
<?php

namespace Database\Seeders\Aval;

use App\Models\Category;
use App\Models\Statuslabel;
use App\Models\User;
use Illuminate\Database\Seeder;

/**
 * Catégories et statuts pour un établissement de santé (config CNC).
 * Idempotent — sûr sur base de production.
 */
class CncCategorySeeder extends Seeder
{
    public function run(): void
    {
        $admin = User::where('permissions->superuser', '1')->first();
        if (! $admin) {
            $this->command?->error('Aucun superadmin — exécuter aval:install ou créer un admin d\'abord.');

            return;
        }

        foreach (['Équipement biomédical', 'Véhicule', 'Informatique', 'Mobilier'] as $name) {
            Category::firstOrCreate(
                ['name' => $name, 'category_type' => 'asset'],
                ['created_by' => $admin->id, 'require_acceptance' => 0, 'checkin_email' => 0]
            );
        }

        $statuses = [
            ['name' => 'En service',     'deployable' => 1, 'pending' => 0, 'archived' => 0],
            ['name' => 'En attente',     'deployable' => 0, 'pending' => 1, 'archived' => 0],
            ['name' => 'En maintenance', 'deployable' => 0, 'pending' => 0, 'archived' => 0],
            ['name' => 'En panne',       'deployable' => 0, 'pending' => 0, 'archived' => 0],
            ['name' => 'Réformé',        'deployable' => 0, 'pending' => 0, 'archived' => 1],
        ];
        foreach ($statuses as $status) {
            Statuslabel::firstOrCreate(
                ['name' => $status['name']],
                $status + ['created_by' => $admin->id, 'show_in_nav' => 0, 'default_label' => 0]
            );
        }
    }
}
```

- [ ] **Step 4 : Implémenter `CncCustomFieldSeeder`**

Créer `database/seeders/Aval/CncCustomFieldSeeder.php` :

```php
<?php

namespace Database\Seeders\Aval;

use App\Models\CustomField;
use App\Models\CustomFieldset;
use App\Models\User;
use Illuminate\Database\Seeder;

/**
 * Champs personnalisés et fieldsets « Véhicule » et « Équipement biomédical ».
 * La sauvegarde d'un CustomField crée la colonne _snipeit_* sur la table assets.
 * Idempotent — firstOrCreate sur le nom, syncWithoutDetaching sur les pivots.
 */
class CncCustomFieldSeeder extends Seeder
{
    public function run(): void
    {
        $admin = User::where('permissions->superuser', '1')->first();
        if (! $admin) {
            $this->command?->error('Aucun superadmin — exécuter aval:install ou créer un admin d\'abord.');

            return;
        }

        $fieldsets = [
            'Véhicule' => [
                ['name' => 'Immatriculation',      'element' => 'text', 'format' => ''],
                ['name' => 'Kilométrage',          'element' => 'text', 'format' => 'numeric'],
                ['name' => 'Échéance assurance',   'element' => 'text', 'format' => 'date'],
                ['name' => 'Échéance vignette',    'element' => 'text', 'format' => 'date'],
                ['name' => 'Chauffeur affecté',    'element' => 'text', 'format' => ''],
            ],
            'Équipement biomédical' => [
                ['name' => 'Criticité clinique',     'element' => 'listbox', 'format' => '', 'field_values' => "Critique\nÉlevée\nMoyenne\nFaible"],
                ['name' => 'Dernière calibration',   'element' => 'text', 'format' => 'date'],
                ['name' => 'Prochaine calibration',  'element' => 'text', 'format' => 'date'],
                ['name' => 'Contrat de maintenance', 'element' => 'text', 'format' => ''],
            ],
        ];

        foreach ($fieldsets as $fieldsetName => $fields) {
            $fieldset = CustomFieldset::firstOrCreate(
                ['name' => $fieldsetName],
                ['created_by' => $admin->id]
            );

            foreach ($fields as $order => $attributes) {
                $field = CustomField::firstOrCreate(
                    ['name' => $attributes['name']],
                    $attributes + ['created_by' => $admin->id]
                );
                $fieldset->fields()->syncWithoutDetaching([
                    $field->id => ['order' => $order + 1, 'required' => 0],
                ]);
            }
        }
    }
}
```

- [ ] **Step 5 : Vérifier le pass**

```bash
php artisan test tests/Feature/Aval/CncSeedersTest.php
```
Attendu : PASS (2 tests). Points de vigilance si échec :
- `firstAdmin()` absent de `UserFactory` → remplacer dans le test par `User::factory()->create(['permissions' => '{"superuser":"1"}'])`.
- Validation `CustomFieldset` exigeant un autre champ → lire le message, ajouter la valeur par défaut.
- Colonne pivot différente → inspecter la migration `custom_field_custom_fieldset` et ajuster (`order`/`required` sont les noms observés dans `CustomFieldSeeder` upstream v8.6.3).

- [ ] **Step 6 : Commit**

```bash
git add database/seeders/Aval/ tests/Feature/Aval/CncSeedersTest.php
git commit -m "feat: add CNC categories, statuses and custom-field seeders"
```

---

### Task 5: Orchestrateur `CncSeeder`

**Files:**
- Create: `database/seeders/Aval/CncSeeder.php`

**Interfaces:**
- Consumes: les trois seeders des Tasks 3–4.
- Produces: `Database\Seeders\Aval\CncSeeder` — point d'entrée unique (`php artisan db:seed --class="Database\Seeders\Aval\CncSeeder"`), utilisé par `aval:install`.

- [ ] **Step 1 : Implémenter (pas de test dédié — composition pure, couverte par le test de Task 6)**

Créer `database/seeders/Aval/CncSeeder.php` :

```php
<?php

namespace Database\Seeders\Aval;

use Illuminate\Database\Seeder;

/**
 * Configuration complète Aval Parc pour le Centre National de Cardiologie.
 * Usage : php artisan db:seed --class="Database\Seeders\Aval\CncSeeder"
 */
class CncSeeder extends Seeder
{
    public function run(): void
    {
        $this->call([
            AvalBrandingSeeder::class,
            CncCategorySeeder::class,
            CncCustomFieldSeeder::class,
        ]);
    }
}
```

- [ ] **Step 2 : Vérification rapide de syntaxe**

```bash
php -l database/seeders/Aval/CncSeeder.php
```
Attendu : `No syntax errors detected`.

- [ ] **Step 3 : Commit**

```bash
git add database/seeders/Aval/CncSeeder.php
git commit -m "feat: add CNC orchestrator seeder"
```

---

### Task 6: Commande `aval:install`

**Files:**
- Create: `app/Console/Commands/Aval/AvalInstall.php`
- Test: `tests/Feature/Aval/AvalInstallTest.php`

**Interfaces:**
- Consumes: `Database\Seeders\Aval\CncSeeder`, modèles `User`, `Setting`. Le `Kernel` upstream charge récursivement `app/Console/Commands/` — aucune inscription nécessaire.
- Produces: commande `aval:install {--admin-username=} {--admin-email=} {--admin-password=}` : migre, crée le superadmin si aucun n'existe, exécute `CncSeeder`. C'est la commande unique d'installation utilisée par `deploy/install.sh` (Task 7).

- [ ] **Step 1 : Écrire le test qui échoue**

Créer `tests/Feature/Aval/AvalInstallTest.php` :

```php
<?php

namespace Tests\Feature\Aval;

use App\Models\Category;
use App\Models\CustomFieldset;
use App\Models\Setting;
use App\Models\User;
use Tests\TestCase;

class AvalInstallTest extends TestCase
{
    public function test_install_command_provisions_admin_settings_and_cnc_data()
    {
        Setting::query()->delete();

        $this->artisan('aval:install', [
            '--admin-username' => 'admin',
            '--admin-email' => 'admin@cnc.mr',
            '--admin-password' => 'ChangeMe!2026',
        ])->assertExitCode(0);

        $admin = User::where('username', 'admin')->first();
        $this->assertNotNull($admin);
        $this->assertTrue($admin->isSuperUser());

        $this->assertEquals('Aval Parc', Setting::first()->site_name);
        $this->assertEquals(4, Category::where('category_type', 'asset')->count());
        $this->assertEquals(2, CustomFieldset::whereIn('name', ['Véhicule', 'Équipement biomédical'])->count());
    }

    public function test_install_command_is_rerunnable_and_keeps_existing_admin()
    {
        Setting::query()->delete();
        $args = [
            '--admin-username' => 'admin',
            '--admin-email' => 'admin@cnc.mr',
            '--admin-password' => 'ChangeMe!2026',
        ];

        $this->artisan('aval:install', $args)->assertExitCode(0);
        $this->artisan('aval:install', $args)->assertExitCode(0);

        $this->assertEquals(1, User::where('username', 'admin')->count());
        $this->assertEquals(1, Setting::count());
    }
}
```

- [ ] **Step 2 : Vérifier l'échec**

```bash
php artisan test tests/Feature/Aval/AvalInstallTest.php
```
Attendu : FAIL — `Command "aval:install" is not defined`.

- [ ] **Step 3 : Implémenter la commande**

Créer `app/Console/Commands/Aval/AvalInstall.php` :

```php
<?php

namespace App\Console\Commands\Aval;

use App\Models\User;
use Database\Seeders\Aval\CncSeeder;
use Illuminate\Console\Command;
use Illuminate\Support\Facades\Hash;

class AvalInstall extends Command
{
    protected $signature = 'aval:install
        {--admin-username= : Identifiant du superadmin initial}
        {--admin-email= : Email du superadmin initial}
        {--admin-password= : Mot de passe du superadmin initial}';

    protected $description = 'Installe Aval Parc : migrations, superadmin, configuration CNC';

    public function handle(): int
    {
        $this->call('migrate', ['--force' => true]);

        if (! User::where('permissions->superuser', '1')->exists()) {
            $username = $this->option('admin-username');
            $email = $this->option('admin-email');
            $password = $this->option('admin-password');

            if (! $username || ! $email || ! $password) {
                $this->error('Aucun superadmin en base : fournir --admin-username, --admin-email et --admin-password.');

                return self::FAILURE;
            }

            User::create([
                'first_name' => 'Admin',
                'last_name' => 'Aval',
                'username' => $username,
                'email' => $email,
                'password' => Hash::make($password),
                'permissions' => '{"superuser":"1"}',
                'activated' => 1,
            ]);
            $this->info("Superadmin « {$username} » créé.");
        } else {
            $this->info('Superadmin existant conservé.');
        }

        $this->call('db:seed', ['--class' => CncSeeder::class, '--force' => true]);

        $this->info('Aval Parc est installé. Connectez-vous pour téléverser le logo (Paramètres > Image de marque).');

        return self::SUCCESS;
    }
}
```

- [ ] **Step 4 : Vérifier le pass, puis la suite Aval complète**

```bash
php artisan test tests/Feature/Aval/AvalInstallTest.php
php artisan test tests/Feature/Aval --stop-on-failure
```
Attendu : PASS partout. Si `User::create` échoue sur une règle de validation (ValidatingTrait), utiliser `$user = new User([...]); $user->save();` et lire le message pour ajouter le champ requis.

- [ ] **Step 5 : Commit**

```bash
git add app/Console/Commands/Aval/ tests/Feature/Aval/AvalInstallTest.php
git commit -m "feat: add aval:install command"
```

---

### Task 7: Déploiement Docker hors-ligne

**Files:**
- Create: `deploy/docker-compose.yml`, `deploy/.env.example`, `deploy/install.sh`, `deploy/backup.sh`, `deploy/export-images.sh`, `deploy/README.md`

**Interfaces:**
- Consumes: `Dockerfile` upstream (build depuis les sources du fork — inclut nos seeders/commande), commande `aval:install`.
- Produces: stack de prod complète. Vérification finale = test manuel de bout en bout en local.

- [ ] **Step 1 : Créer `deploy/docker-compose.yml`**

```yaml
# Aval Parc — production (serveur local clinique)
# Démarrage : docker compose --env-file .env up -d

volumes:
  db_data:
  storage:

services:
  app:
    build:
      context: ..
      dockerfile: Dockerfile
    image: aval-parc:${AVAL_VERSION:-v1}
    restart: unless-stopped
    volumes:
      - storage:/var/lib/snipeit
    ports:
      - "${APP_PORT:-8000}:80"
    depends_on:
      db:
        condition: service_healthy
        restart: true
    env_file:
      - .env

  db:
    image: mariadb:11.4.7
    restart: unless-stopped
    volumes:
      - db_data:/var/lib/mysql
    environment:
      MYSQL_DATABASE: ${DB_DATABASE}
      MYSQL_USER: ${DB_USERNAME}
      MYSQL_PASSWORD: ${DB_PASSWORD}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 5s
      timeout: 1s
      retries: 5
```

- [ ] **Step 2 : Créer `deploy/.env.example`**

Partir du `.env.docker` upstream (le copier puis ajuster) avec au minimum :

```bash
cp ../.env.docker .env.example
```
Puis éditer `deploy/.env.example` pour forcer ces valeurs (garder le reste du fichier upstream tel quel) :
```
APP_ENV=production
APP_DEBUG=false
APP_URL=http://aval-parc.local:8000
APP_LOCALE=fr-FR
APP_TIMEZONE=Africa/Nouakchott
DB_CONNECTION=mysql
DB_HOST=db
DB_DATABASE=avalparc
DB_USERNAME=avalparc
DB_PASSWORD=CHANGEZ_MOI
MYSQL_ROOT_PASSWORD=CHANGEZ_MOI_AUSSI
APP_PORT=8000
```
(`APP_KEY` reste vide : généré par `install.sh`.)

- [ ] **Step 3 : Créer `deploy/install.sh`**

```bash
#!/usr/bin/env bash
# Installation initiale d'Aval Parc sur le serveur de la clinique.
# Usage : ./install.sh <admin-username> <admin-email>
set -euo pipefail
cd "$(dirname "$0")"

[ $# -eq 2 ] || { echo "Usage: $0 <admin-username> <admin-email>"; exit 1; }

if [ ! -f .env ]; then
  cp .env.example .env
  echo ">>> .env créé. Éditez les mots de passe (CHANGEZ_MOI) puis relancez."
  exit 1
fi

read -r -s -p "Mot de passe du superadmin : " ADMIN_PASSWORD; echo

docker compose up -d --build
echo ">>> Attente de la base de données..."
sleep 15

if ! grep -q '^APP_KEY=base64' .env; then
  KEY=$(docker compose exec -T app php artisan key:generate --show)
  sed -i.bak "s|^APP_KEY=.*|APP_KEY=${KEY}|" .env
  docker compose up -d app
  sleep 10
fi

docker compose exec -T app php artisan aval:install \
  --admin-username="$1" --admin-email="$2" --admin-password="$ADMIN_PASSWORD"

echo ">>> Aval Parc est prêt : http://localhost:$(grep -E '^APP_PORT=' .env | cut -d= -f2 || echo 8000)"
```

- [ ] **Step 4 : Créer `deploy/backup.sh`**

```bash
#!/usr/bin/env bash
# Sauvegarde quotidienne : base + fichiers. Rotation 14 jours.
# Cron suggéré : 0 2 * * * /opt/aval-parc/deploy/backup.sh
set -euo pipefail
cd "$(dirname "$0")"
BACKUP_DIR="${BACKUP_DIR:-./backups}"
STAMP=$(date +%Y-%m-%d_%H%M)
mkdir -p "$BACKUP_DIR"

docker compose exec -T db sh -c 'mariadb-dump -u root -p"$MYSQL_ROOT_PASSWORD" "$MYSQL_DATABASE"' \
  | gzip > "$BACKUP_DIR/db_${STAMP}.sql.gz"

docker compose exec -T app tar -czf - /var/lib/snipeit > "$BACKUP_DIR/storage_${STAMP}.tar.gz"

find "$BACKUP_DIR" -name '*.gz' -mtime +14 -delete
echo "Sauvegarde OK : $BACKUP_DIR (db + storage, ${STAMP})"
```

- [ ] **Step 5 : Créer `deploy/export-images.sh`**

```bash
#!/usr/bin/env bash
# Exporte les images Docker pour un serveur sans accès internet (transfert USB).
# Sur le serveur : docker load -i aval-parc-images.tar
set -euo pipefail
cd "$(dirname "$0")"
docker compose build
docker save aval-parc:${AVAL_VERSION:-v1} mariadb:11.4.7 -o aval-parc-images.tar
echo "Images exportées dans deploy/aval-parc-images.tar ($(du -h aval-parc-images.tar | cut -f1))"
```

- [ ] **Step 6 : Créer `deploy/README.md`**

```markdown
# Déploiement Aval Parc

## Installation (serveur clinique, Debian/Ubuntu + Docker)

1. Copier le dépôt sur le serveur (git clone, ou USB si hors-ligne).
2. Si hors-ligne : charger les images préparées avec `export-images.sh` :
   `docker load -i deploy/aval-parc-images.tar`
3. `cd deploy && ./install.sh admin admin@cnc.mr`
   - Premier lancement : le script crée `.env` — éditer les mots de passe puis relancer.
4. Ouvrir `http://<ip-serveur>:8000`, se connecter, téléverser le logo
   (Paramètres → Image de marque).

## Sauvegardes

- `./backup.sh` — base + fichiers dans `deploy/backups/`, rotation 14 jours.
- Ajouter au cron : `0 2 * * * /opt/aval-parc/deploy/backup.sh`
- **Copier régulièrement `deploy/backups/` sur un disque externe.**

## Restauration

1. `docker compose up -d db`
2. `gunzip -c backups/db_<date>.sql.gz | docker compose exec -T db sh -c 'mariadb -u root -p"$MYSQL_ROOT_PASSWORD" "$MYSQL_DATABASE"'`
3. `docker compose up -d app` puis restaurer storage :
   `gunzip -c backups/storage_<date>.tar.gz | docker compose exec -T app tar -xzf - -C /`

## Mise à jour

1. Sauvegarder (`./backup.sh`).
2. `git pull` (ou charger la nouvelle image par USB).
3. `docker compose up -d --build`
4. `docker compose exec app php artisan migrate --force`
```

- [ ] **Step 7 : Rendre les scripts exécutables et vérifier la syntaxe**

```bash
chmod +x deploy/*.sh
bash -n deploy/install.sh deploy/backup.sh deploy/export-images.sh
```
Attendu : aucune sortie (syntaxe valide).

- [ ] **Step 8 : Test de bout en bout local**

```bash
cd deploy && cp .env.example .env
sed -i.bak 's/CHANGEZ_MOI_AUSSI/rootpass-local-test/; s/CHANGEZ_MOI/avalpass-local-test/' .env
./install.sh admin admin@cnc.mr   # mot de passe de test à la demande
curl -s http://localhost:8000/login | grep -io "aval parc" | head -1
```
Attendu : le build passe (long la première fois), `aval:install` s'exécute, et le HTML de la page de connexion contient « Aval Parc ». Ensuite vérifier dans le navigateur : login admin, catégories CNC présentes, fieldsets « Véhicule » / « Équipement biomédical » visibles dans Paramètres → Champs personnalisés.
Nettoyage : `docker compose down` (garder les volumes pour re-tester, `-v` pour tout effacer).

- [ ] **Step 9 : Commit**

```bash
git add deploy/
git commit -m "feat: add offline-capable Docker production deployment"
```

---

### Task 8: Documentation du fork + vérification finale

**Files:**
- Create: `docs/UPSTREAM_PATCHES.md`, `docs/MISE_A_JOUR_UPSTREAM.md`, `README-AVAL.md`

**Interfaces:**
- Consumes: tout ce qui précède.
- Produces: documentation opérationnelle du fork ; suite de tests complète verte ; push final.

- [ ] **Step 1 : Créer `docs/UPSTREAM_PATCHES.md`**

```markdown
# Registre des patchs upstream

Fichiers Snipe-IT modifiés par Aval Parc. **À re-vérifier à chaque merge d'une release upstream.**

| Fichier | Raison | Comment vérifier après merge |
|---|---|---|
| _(aucun en v1)_ | | |

Règle : ne modifier un fichier upstream qu'en dernier recours (niveau 3 de la spec).
Toute entrée ajoutée ici doit expliquer la raison et le test de non-régression associé.
```

- [ ] **Step 2 : Créer `docs/MISE_A_JOUR_UPSTREAM.md`**

```markdown
# Mise à jour depuis Snipe-IT (fork suiveur)

À chaque release Snipe-IT (tag `vX.Y.Z`) :

    git fetch upstream --tags
    git checkout upstream-master && git merge vX.Y.Z --ff-only
    git checkout main && git merge vX.Y.Z

1. Résoudre les éventuels conflits (rares : nos fichiers sont isolés
   dans `database/seeders/Aval/`, `app/Console/Commands/Aval/`, `deploy/`, `docs/`).
2. Re-vérifier chaque entrée de `docs/UPSTREAM_PATCHES.md`.
3. `composer install && php artisan test tests/Unit tests/Feature/Aval --stop-on-failure`
4. Checklist manuelle de non-régression (voir ci-dessous) sur un déploiement local.
5. Push, puis mise à jour clinique : sauvegarde → nouvelle image → `migrate --force`.

## Checklist de non-régression (avant tout déploiement CNC)

- [ ] Login superadmin
- [ ] Création d'un actif dans « Équipement biomédical » avec ses champs custom
- [ ] Checkout / checkin d'un actif vers un utilisateur
- [ ] Rapport : liste des actifs exportée (CSV)
- [ ] Impression d'une étiquette
- [ ] Page « À propos » : nom « Aval Parc », footer avec lien code source
```

- [ ] **Step 3 : Créer `README-AVAL.md`**

```markdown
# Aval Parc

Gestion de parc pour établissements de santé : équipements biomédicaux,
véhicules, informatique, mobilier. Basé sur [Snipe-IT](https://snipeitapp.com)
(AGPL-3.0), maintenu en fork suiveur.

Premier déploiement : Centre National de Cardiologie, Nouakchott, Mauritanie.

- **Spécification :** `docs/superpowers/specs/2026-07-04-aval-parc-design.md`
- **Installation :** `deploy/README.md`
- **Mise à jour upstream :** `docs/MISE_A_JOUR_UPSTREAM.md`
- **Licence :** AGPL-3.0 (voir `LICENSE`). Le code source complet de cette
  version, modifications incluses, est disponible sur ce dépôt.

## Installation rapide (dev)

    composer install
    cp .env.example .env && php artisan key:generate
    # configurer la base dans .env, puis :
    php artisan aval:install --admin-username=admin --admin-email=vous@exemple.mr --admin-password=...

## Tests

    php artisan test tests/Feature/Aval
```

- [ ] **Step 4 : Vérification finale complète**

```bash
php artisan test tests/Feature/Aval --stop-on-failure
php artisan test tests/Unit --stop-on-failure
```
Attendu : tout PASS.

- [ ] **Step 5 : Commit et push**

```bash
git add docs/UPSTREAM_PATCHES.md docs/MISE_A_JOUR_UPSTREAM.md README-AVAL.md
git commit -m "docs: add fork maintenance docs and Aval Parc readme"
git push origin main
```

---

## Notes pour l'exécutant

- **Ne jamais modifier un fichier upstream.** En cas de blocage apparent, chercher d'abord le mécanisme natif Snipe-IT (settings, custom fields, events/listeners), sinon documenter dans `UPSTREAM_PATCHES.md` et demander validation.
- Les noms de champs custom sont en français et **exactement** ceux des tests (accents compris) — Snipe-IT gère l'UTF-8 dans les noms via slug pour les colonnes.
- Le build Docker initial est long (composer + assets) : prévoir 10–20 min.
- v1.1 (traductions), v2 (rapports véhicules), v3 (biomédical avancé) feront l'objet de plans séparés.
